{
  "name": "workspaces",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "publish": "lerna run publish --ignore=www",
    "build": "lerna run build --ignore=www",
    "build:nextjs": "lerna run build --scope=www",
    "dev": "lerna run dev --include-dependencies --scope=www --stream"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
